name: CI/CD - Build & Deploy

concurrency:
  group: deploy-student3
  cancel-in-progress: true

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      IMAGE_NAME: spring-app-student3
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      APP_DIR: /home/backend/spring-app/student3

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: "21"
          distribution: "temurin"

      - name: Build application
        run: ./mvnw -B clean package -DskipTests

      # 🐳 Docker Login
      - name: Login to DockerHub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 🐳 Build & Push
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: |
            ${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest

      # 🔍 DEBUG (TARUH DI SINI)
      - name: Debug Secrets
        run: |
          echo "HOST=${{ secrets.VPS_HOST }}"
          echo "USER=${{ secrets.VPS_USER }}"

      # 📦 Copy compose
      - name: Copy docker-compose to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "docker-compose.yml"
          target: "${{ env.APP_DIR }}"
          overwrite: true

      # 🚀 DEPLOY ON VPS (PULL & RUN ONLY)
      - name: Deploy on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd ${{ env.APP_DIR }}
            echo "🚀 Starting deployment..."
            
            echo "📝 Creating .env file..."
            echo "DOCKERHUB_USERNAME=${{ secrets.DOCKERHUB_USERNAME }}" > .env
            echo "APP_PORT=9003" >> .env
            
            # 🔥 INI BAGIAN KAMU
            echo "DB_URL=jdbc:mysql://mysql:3306/db_spring_student3?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC" >> .env
            echo "DB_NAME=db_spring_student3" >> .env
            echo "DB_USER=backend" >> .env
            echo "DB_PASSWORD=rahasia_banget" >> .env
      
            echo "📥 Pulling latest image..."
            docker compose pull

            echo "🔄 Restarting services..."
            docker compose down
            docker compose up -d

            echo "⏳ Waiting for health check..."
            sleep 30

            # Verify deployment
            if docker compose ps | grep -q "Up"; then
              echo "✅ Deployment completed successfully!"
              docker compose ps
            else
              echo "❌ Deployment failed!"
              docker compose logs
              exit 1
            fi
